name: CI

on:
  push:


jobs:

  Hello-world:

    runs-on: ubuntu-latest

    steps:
    #- uses: actions/checkout@v4
    
    - name: Hello-world
      run: echo "Hola Mundo"
  
  Hello-world2:
    needs: Hello-world
    runs-on: ubuntu-latest

    steps:
    #- uses: actions/checkout@v4
    
    - name: Hello-world
      run: echo "Hola Mundo"
